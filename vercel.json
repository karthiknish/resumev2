{
  "version": 2,
  "buildCommand": "npm install typescript @types/react @types/node --save-dev && npm install --legacy-peer-deps && npm run build",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
